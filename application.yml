spring:
  profiles:
    active:
      - dev

---    
spring:
  profiles: dev
  application:
    name: microservice-springcloud-config-dev

---
spring:
  profiles: test
  application:
    name: microservice-springcloud-config-test 